using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class yAxisBlocks : MonoBehaviour
{
    // detect when the block gets destroyed
    // work out the y axis of where the new block will go
    // if the block is destroyed instantiate a new block in position directly below it
    void Start()
    {

    }
    void Update()
    {

    }
}
